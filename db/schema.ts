import {sqliteTable,text,integer,index} from 'drizzle-orm/sqlite-core';
export const metrics=sqliteTable('metrics',{groupId:text('group_id').primaryKey(),total:integer('total').notNull(),renewed:integer('renewed'),updated:text('updated').notNull(),source:text('source').notNull(),userId:text('user_id').notNull()});
export const entries=sqliteTable('entries',{id:text('id').primaryKey(),kind:text('kind').notNull(),groupId:text('group_id').notNull(),title:text('title').notNull(),reason:text('reason').notNull(),method:text('method').notNull(),result:text('result').notNull(),status:text('status').notNull(),followUp:text('follow_up').notNull(),fileId:text('file_id'),fileName:text('file_name'),created:text('created').notNull(),author:text('author').notNull(),userId:text('user_id').notNull()},t=>[index('idx_entries_created').on(t.created)]);
