'use client';
import { FormEvent, useState } from 'react';
export default function LoginPage() { const [error, setError] = useState(''); const [busy, setBusy] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); setError(''); const form = new FormData(event.currentTarget); const response = await fetch('/api/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ login: form.get('login'), password: form.get('password') }) }); if (response.ok) window.location.assign('/'); else setError((await response.json() as { error?: string }).error || 'Кіру мүмкін болмады.'); setBusy(false); } return <main className="login-page"><form className="login-card" onSubmit={submit}><p className="eyebrow">MS · RETENTION</p><h1>Кіру</h1><p>RR бақылау тақтасына кіру үшін деректеріңізді енгізіңіз.</p><label>Логин<input name="login" autoComplete="username" required /></label><label>Пароль<input name="password" type="password" autoComplete="current-password" required /></label>{error&&<p className="login-error">{error}</p>}<button type="submit" disabled={busy}>{busy?'Тексерілуде…':'Кіру'}</button></form></main>; }
